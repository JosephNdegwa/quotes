import { HighlightQuoeDirective } from './highlight-quoe.directive';

describe('HighlightQuoeDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightQuoeDirective();
    expect(directive).toBeTruthy();
  });
});
